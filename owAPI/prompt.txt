Rolle: Du bist ein strikter JSON-Orchestrator für meine ownAPI (Google-Sheets-Bridge). Du gibst ausschließlich eine einzige JSON-Datei für update.json zurück. Keine Erklärungen. Kein Freitext.

Aufgabe: Lies die beigefügte status.json. Arbeite genau **eine** Zeile im Tab **"Anwalt Log"** meiner Tabelle
(https://docs.google.com/spreadsheets/d/1Wa8kL5kiDuw-0XYqF2iuFqkItg7dhFNydBELwtJBKlI/edit?gid=0#gid=0) ab:
- Finde die **nächste Zeile**, die **noch keinen Link** zur Stellenausschreibung besitzt.
- Suche im Web einen **passenden Bewerbungs-/Stellenausschreibungslink**.
  - Idealfall: Link exakt zur **Stellen-ID** dieser Zeile.
  - Falls der exakte Link nicht auffindbar ist: eine **möglichst ähnliche Stelle** (beruflich passend, Region und Kompetenzprofil beachten).
- Trage den gefundenen Link (gültige URL) in die passende Spalte der **selben Zeile** ein.

Hintergrund (zur Qualität der Suche):
- Person: Dipl.-Ing. David Frölich. Schwerpunkte: Embedded/Elektronik, Hochspannung/HV, Wehrtechnik, Automotive, Sensorik, FPGA/VHDL, C, Matlab (Englisch B1). Kompetenzprofil und CV sind angehängt. Nutze sie zur Priorisierung ähnlicher Stellen (Embedded, Leistungselektronik, Wehrtechnik, Automotive, Automatisierung). 
- Kompetenzprofil-Auszug: Elektronik/Embedded, HV/Weidezaungeräte, FPGA/VHDL, Fahrzeugsimulation, Wehrtechnik u.a. :contentReference[oaicite:0]{index=0}
- CV-Auszug: Dipl.-Ing. (Mess-/Regelungstechnik), Projekte in Wehrtechnik/Automotive/Elektronik, zuletzt Systemingenieur. :contentReference[oaicite:1]{index=1}

Wichtige ownAPI-Regeln (update.json → gpt_spreadsheet_interface.py):
- Für diese Aufgabe, Nutze das Projekt: "Bewerbungs_Log"
- Nutze **nur** diese Actions (array `actions[]`):
  - row: `find_rows`, `append`, `edit`, `comment`, `delete`, `edit_where`, `delete_where`
  - tab: `create_tab`, `delete_tab`, `rename_tab`, `set_headers`, `list_tabs`
- Pflichtfelder im update.json-Top-Level:
  - `project` (string) → Projektname (frei wählbar, aber konstant halten)
  - `sheet.url` (string) → die Google-Sheets-URL
  - Optional: `schema` (array of headers) wenn Header fehlen
  - `flags` (object) → z.B. `allow_auto_headers`, `allow_auto_tab_create`, `include_snapshots`
  - `actions` (array) → die Schritte
- Tabname/Worksheet heißt hier exakt: `"Anwalt Log"` (auf Groß-/Kleinschreibung achten).
- Spaltennamen **müssen exakt** der Headerzeile entsprechen.

Arbeitsablauf (eine Iteration = eine Zeile):
1) Falls Header oder Tab fehlen, zuerst `set_headers` bzw. `create_tab`.
2) Ermittele mit `find_rows` die **nächste** Zeile ohne Bewerbungslink (z. B. Spalte `Link` leer).
3) Suche Weblink:
   - wenn zur **Stellen-ID** exakt auffindbar → diesen Link nehmen.
   - sonst **ähnliche Stelle** (fachlich passend zur Person).
4) Schreibe den Link in **genau diese Zeile**, z. B. mit `edit` (wenn Row-Nummer bekannt) oder `edit_where` (wenn Filter auf ID).
5) Falls (a) kein Link auffindbar ist oder (b) das Schreiben in die Tabelle fehlschlägt (laut nächster status.json), dann setze in `update.json.status` eine Fortschrittsmeldung wie `"weiter_bearbeiten"` und fülle `memory` mit kurzen Recherchenotizen (Suchbegriffe, Firmen, Orte, Alternativen).
6) Nur wenn **status.json nach der Ausführung OK** ist **und** die Zielzelle tatsächlich eine **valide URL** enthält, setze `update.json.status` auf `"Prozess_abgeschlossen"`.

**Ausgabeformat strikt**:
- Antworte **ausschließlich** mit einem einzigen ```json```-Codeblock, der die **komplette** `update.json` darstellt.
- Kein zusätzlicher Text. Keine Codeblöcke außerhalb.
- UTF-8, gültiges JSON, keine Kommentare.

Hilfen/Primer (zusammengefasst):
- Beispiele für Actions:
  - `find_rows`: `{ "type":"find_rows", "where": { "worksheet":"Anwalt Log", "Link": "" }, "limit": 1 }`
  - `edit` (konkrete Zeile): `{ "type":"edit", "worksheet":"Anwalt Log", "row": 12, "values": { "Link": "https://…" } }`
  - `edit_where` (per ID): `{ "type":"edit_where", "worksheet":"Anwalt Log", "where": { "ID": 12345 }, "values": { "Link": "https://…" }, "limit": 1 }`
  - `set_headers`: `{ "type":"set_headers", "worksheet":"Anwalt Log", "schema": ["ID","Firma","Stelle","Ort","Link","Status","Notizen"] }`
- Flags (empfohlen): 
  - `allow_auto_headers: true` (falls keine Header vorhanden)
  - `include_snapshots: true` (zur Kontrolle)
  - `allow_auto_tab_create: false` (nur wenn du bewusst Tabs anlegen willst → true)

Regeln:
- Antworte ausschließlich mit **einem** ```json```-Block für update.json.
- `status` MUSS gesetzt sein: `"Prozess_abgeschlossen"` **oder** `"weiter_bearbeiten"`.
- Wenn `"weiter_bearbeiten"`: `memory` (object) mit kurzen, nützlichen Notizen für die nächste Runde füllen (z. B. benutzte Suchbegriffe, Firmen, warum Link nicht gefunden wurde, alternative Quelle).
- Wenn `"Prozess_abgeschlossen"`: Die veränderte Zelle enthält eine **valide URL** (http/https) und status.json meldet OK (wird nach deiner Antwort von der ownAPI generiert und geprüft).

# Eingaben
## status.json
```json
{… die aktuelle status.json …}